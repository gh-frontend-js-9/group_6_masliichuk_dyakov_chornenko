import React from "react";
/*
import { useDispatch, useSelector} from "react-redux";
import {fetchPhotos} from "../redux/action/action";
import Photo from "./Photo";


interface State {
    photos: any
}

const ElementPhotoData = () => {
    const dispatch = useDispatch();
    const photos = useSelector((state: State) => state.photos.fetchedPhotos);

    if (!photos.length) {
        dispatch(fetchPhotos());
    }
    return photos.map((photo: any)=>  return (
        <div >
            <img src={photo.url} alt={photo.title}/>
        </div>)
};
export default ElementPhotoData;*/
